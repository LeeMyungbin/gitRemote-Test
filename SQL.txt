SQL
Database -> 운영/ 공용 / 통합 / 저장 데이터
DBMS -> Database Management System
              정의, 저장, 수정, 삭제, 조회 ...
MySQL (DBMS의 한 종류)
RDBMS -> 관계형 데이터베이스


SQL ( 모든 DBMS가 사용하는 문법, 약간씩 차이)
DDL
DML (DQL)
DCL (TCL)

SELECT
          컬럼명 , 연산결과, 함수 AS 별칭
FROM
        테이블명 
WHERE
       조건 필터링
ORDER BY
      정렬 조건
LIMIT



PRIMARY KEY (PK)                                                                          AUTO_INCREMENT : 자동으로 숫자 부여
-> 중복 X, NULL X -> 행을 구별 식별자              

                                                   table
            insert ->                        user -> 계정 (아이디, 비밀번호)
          insert * ->                       user_profile -> 프로필 사진 n개   
          insert * n ->                    user_address -> 배송 주소 n 개



transaction control language (tcl)
commit
rollback
                                  자식                        부모

tbl_menu                   fk                                     tbl_category
                        category_code                        category_code
1.김찌찌개         1                                         1                            한식
2.파스타            3                                         2                            중식
3.짬뽕               4                                         3                            양식

select...
from...
where name="홍길동"
table


[index]
주소 -> row id (물리적 위치)
select *
from users
where user_id = 128756                                  28756 -> row id

primary key -> 자동 인덱스 생성























       